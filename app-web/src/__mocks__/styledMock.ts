const styled = () => (component: any) => component;

styled.div = styled;
styled.h1 = styled;
styled.h2 = styled;
styled.h3 = styled;
styled.h4 = styled;
styled.button = styled;
styled.input = styled;
styled.svg = styled;
styled.css = styled;
styled.keyframes = styled;

module.exports = styled;